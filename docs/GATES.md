# Current gates

These gates replace the calendar promises in BUILD_PLAN.md.

1. **Static foundation:** browser-only output, deployment workflow and host contract, reducer, explicit rules, state snapshots, tests, local production preview. Implemented. Public-host and actual-Chromebook checks remain open.
2. **First complete Alerts route:** builder through debrief, evidence, failure/retry, keyboard controls, claim feedback. Implemented and locally exercised; learner timing and full content audit remain open.
3. **Procedural expansion:** chair stalls, conference, presidential inaction, broader fixture coverage, more substantial hearing/markup choices, and any justified uncertainty. Pending.
4. **Content expansion and submission:** two additional issues with all 36 initial card/priority states, full vocabulary/evidence coverage, school test, final release and rubric capture. Pending.

Every gate must pass tests and produce the same deployable static artifact. No feature may introduce a runtime service dependency.

Interaction rebuild: the implemented Alerts route now runs through seven explorable rooms with NPC dialogue, world travel, optional evidence, and a required archive debrief. See `INTERACTION_REBUILD.md`. The original visual polish and broader legislative content remain separate milestones.
